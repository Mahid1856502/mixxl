services:
  api:
    build: .
    container_name: mixxl-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "5000"
      SERVE_STATIC: "false"
